{
  "rule_id": "TRUFFLE-SQL-001",
  "name": "Raw SQL Query with String Concatenation",
  "severity": "high",
  "languages": ["python", "javascript", "go", "cpp"],
  "match_type": "regex",
  "patterns": [
    "(?i)cursor\\.execute\\(['\"].*\\+",
    "(?i)cursor\\.execute\\(f['\"]",
    "(?i)WHERE\\s+.*\\s*=\\s*['\"].*\\+\\s*"
  ],
  "description": "检测到 SQL 查询使用字符串拼接构建，存在 SQL 注入风险",
  "recommendation": "使用参数化查询，例如 cursor.execute('SELECT * FROM users WHERE id = ?', (user_id,))",
  "category": "SQL Injection"
}

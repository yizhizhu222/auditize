# 从零搭建一个公网可访问的网站——全流程解释

> 用 trufflekit.com 为例，解释每一步做什么、为什么这么做。

---

## 一、买域名——什么是域名？

### 做了什么
在阿里云花 85 元买了 `trufflekit.com`

### 为什么需要这一步

```
互联网上的每台电脑都有一个 IP 地址，比如 58.16.61.185
但你记不住这串数字，就像你记不住朋友的身份证号一样

域名就是给 IP 取一个好记的名字：
  58.16.61.185 → 没人记得住
  trufflekit.com → 一眼就记住
```

**域名系统（DNS）的本质**：一本电话本。你输入 `trufflekit.com`，浏览器去查"这个域名对应哪个 IP"，然后跳过去。

### 为什么在阿里云买
- 阿里云是国内公司，支持支付宝，不需要外币信用卡
- 阿里云帮你向域名管理局注册，每年续费就行
- 买了之后这个域名 365 天归你管

---

## 二、实名认证——为什么买域名要实名？

### 做了什么
上传身份证正反面，等 1-2 小时审核通过

### 为什么需要这一步
中国法律规定：所有境内注册的域名必须实名认证。这是为了防止网络诈骗、钓鱼网站。

**没有实名 → 域名无法使用。**

---

## 三、DNS 转到 Cloudflare——什么是 DNS 服务器？

### 做了什么
把阿里云的 DNS 服务器（`dns10.hichina.com`）改成 Cloudflare 的（`elinore.ns.cloudflare.com` / `wilson.ns.cloudflare.com`）

### 为什么需要这一步

```
DNS 服务器 = 电话本的"管理员"

阿里云默认的 DNS 服务器只能做最基础的事情：
  把 trufflekit.com 指向一个 IP 地址

Cloudflare 的 DNS 服务器除了指向 IP，还能做：
  ✅ 自动 HTTPS 证书（免费）
  ✅ DDoS 防护（防攻击）
  ✅ Tunnel 代理（不需要公网 IP）
  ✅ 全球加速（CDN）
```

**转 DNS 就像换电话本管理员——原来的只能记地址，新的还能帮你收快递、防骚扰。**

---

## 四、Cloudflare Tunnel——什么是隧道？

### 做了什么
在 Windows 上安装 cloudflared，用 token 连到 Cloudflare，创建一条隧道指向 `localhost:8000`

然后在 Cloudflare 网页上配：`trufflekit.com → localhost:8000`

### 为什么需要这一步——这是最核心的！

```
传统的网站部署方式：
  你有一台云服务器（比如阿里云 ECS，月租 50+ 元）
  你的代码跑在这台服务器上
  用户访问 trufflekit.com → Cloudflare → 你的服务器

  问题：你要花钱租服务器，要自己维护，出了问题要自己修。

Tunnel 的方式：
  你的代码跑在你自己的电脑上（WSL）
  Cloudflare Tunnel 在你的电脑和 Cloudflare 之间开了一条"秘密通道"
  用户访问 trufflekit.com → Cloudflare → 通道 → 你电脑的 localhost:8000

  不需要云服务器，不需要公网 IP，不需要开放端口。
```

### Tunnel 解决了什么实际问题

**中国运营商的问题：**
你家宽带的公网 IP（58.16.61.185）是被运营商封锁的——外网连不进来。
你试过端口转发、防火墙规则都不行，因为运营商在源头就拦截了。

**Tunnel 的绕过方式：**
```
普通访问：用户 → 你的 IP → 被运营商拦截 ✗

Tunnel 访问：用户 → Cloudflare（国外服务器）→
            Cloudflare 主动连你的电脑 →
            你电脑上的 cloudflared 收到请求 →
            转发到 localhost:8000 ✓
```

关键区别：**不是外网连你，而是你主动连 Cloudflare。** 中国运营商不拦截出站连接，只拦截入站连接。

---

## 五、整个链路总结

```
用户打开浏览器输入 https://trufflekit.com
    │
    ▼
DNS 查询（Cloudflare 管理）
    │  "trufflekit.com 在哪？"
    ▼
Cloudflare DNS 返回："走 Tunnel"
    │
    ▼
Cloudflare Edge 服务器
    │  找到你电脑上运行的 cloudflared 进程
    ▼
Tunnel 通道（你的电脑 ←→ Cloudflare）
    │  cloudflared 把请求转发到 localhost:8000
    ▼
你的后端（uvicorn）
    │  处理请求，返回网页
    ▼
Tunnel 通道返回
    ▼
用户看到你的网站
```

### 中间经过了哪些免费服务

| 服务 | 费用 | 作用 |
|------|------|------|
| 阿里云域名 | 85 元/年 | 给你一个名字 |
| Cloudflare DNS | 免费 | 告诉全世界你的域名归谁管 |
| Cloudflare Tunnel | 免费 | 把你的电脑和 Cloudflare 连起来 |
| Cloudflare SSL | 免费 | 自动给你 HTTPS 证书 |
| 你家宽带 | 你本来就在付 | 跑你的代码 + cloudflared |
| **总计** | **85 元/年**（约 7 元/月） | |

### 为什么这么便宜

因为你**没有买云服务器**。

传统的做法：
```
域名 85 元/年 + 云服务器 600 元/年 = 685 元/年
```

你的做法：
```
域名 85 元/年 + Tunnel 免费 + 用自己的电脑 = 85 元/年
```

代价是你的电脑不能关机——关了网站就访问不了。但开发阶段完全够用了。

---

## 六、关于 ngrok

在买到域名之前，你用的是 ngrok。

```
ngrok：你给我一个 https://xxx.ngrok-free.dev 的地址
       不需要买域名、不需要配 DNS
       但地址难记、每次重启会变

Cloudflare Tunnel + 域名：https://trufflekit.com
       地址固定、有自己的名字、带 HTTPS
       多了一步买域名和配 DNS 的工作
```

两者技术原理一样——都是在你电脑上跑一个代理程序，主动连到云服务商。区别在于 ngrok 不让你绑定自己的域名（免费版）。

---

## 七、一张图总结

```
                      ┌──────────────────┐
                      │  用户浏览器       │
                      │  trufflekit.com  │
                      └────────┬─────────┘
                               │
                               ▼
                      ┌──────────────────┐
                      │  Cloudflare DNS  │  你花钱买了域名
                      │  (自动 HTTPS)   │  你把 DNS 转到这里
                      └────────┬─────────┘
                               │
                               ▼
                      ┌──────────────────┐
                      │  Cloudflare      │  免费服务
                      │  Tunnel 边缘节点 │
                      └────────┬─────────┘
                               │
                    ┌──────────┴──────────┐
                    │  加密隧道           │
                    │  cloudflared 运行中  │  你电脑上的程序
                    └──────────┬──────────┘
                               │
                               ▼
                      ┌──────────────────┐
                      │  localhost:8000  │  你的项目
                      │  (uvicorn)       │
                      └──────────────────┘
```

**你不需要：** 云服务器、公网 IP、开放端口、备案、HTTPS 证书。
**你只需要：** 一个域名 + 一台能上网的电脑 + Cloudflare 免费账号。

这就是为什么 85 元就能让一个项目上线。
